tpmrnd

tpmrnd is a daemon that writes entropy from the TPM to the host once
per day. It supports the following flags:

	-f		Run in the foreground (do not daemonise).

	-s seconds	Specify the update period in seconds.

Messages are written to syslog if tpmrnd is running as a daemon, or to
the console if it is running in the fore‐ ground.

tpmrnd is released under an ISC license.
